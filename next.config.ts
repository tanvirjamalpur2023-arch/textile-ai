import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Vercel handles this automatically, but useful for Docker/standalone deploys
  // output: "standalone",
};

export default nextConfig;
